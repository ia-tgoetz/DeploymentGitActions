Deploy Options:

git clone https://github.com/ia-tgoetz/DeploymentGitActions.git
cd DeploymentGitActions
sudo bash scripts/provision-runner.sh {gitActionToken}


stage system Remove Runner:

cd /opt/actions-runner
sudo ./svc.sh stop
sudo ./svc.sh uninstall

sudo rm -rf /opt/actions-runner
sudo userdel -r github-runner
cd /home/$USER/DeploymentGitActions
GATEWAY_ADMIN_USERNAME=admin GATEWAY_ADMIN_PASSWORD=hfL3nvK9JmYntvBRjoXp docker compose down -v
cd /home/$USER
rm -rf DeploymentGitActions


Change Hostname


docker compose down
GATEWAY_ADMIN_USERNAME=admin GATEWAY_ADMIN_PASSWORD=password docker compose down -v

https://github.com/ia-tgoetz/DeploymentGitActions/actions/workflows/deploy.yml

